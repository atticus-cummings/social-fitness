import React from "react";

export default function WorkoutPost({ supabase, session }) {
    return(
        <div>
            <div>Workout </div>
            <div>This info will be inputted into profile. You'll have an option to automattically post a formatted version here</div>
        </div>
    )
}